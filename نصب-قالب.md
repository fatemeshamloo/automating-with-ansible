# راهنمای نصب قالب هنر ایران

## مراحل نصب

### ۱. آماده‌سازی فایل‌ها

اول از همه، این فایل‌ها را در پوشه قالب خود داشته باشید:

```
artofiran/
├── style.css
├── index.php
├── functions.php (یا functions-minimal.php)
├── rtl.css
├── woocommerce/
│   └── content-product.php
└── README.md
```

### ۲. انتخاب نسخه functions.php

**برای جلوگیری از خطای critical، از فایل ساده‌تر استفاده کنید:**

1. فایل `functions.php` کنونی را به `functions-full.php` تغییر نام دهید
2. فایل `functions-minimal.php` را به `functions.php` تغییر نام دهید

```bash
mv functions.php functions-full.php
mv functions-minimal.php functions.php
```

### ۳. نصب قالب در WordPress

1. فایل‌های قالب را در مسیر زیر کپی کنید:
   ```
   /wp-content/themes/artofiran/
   ```

2. وارد پنل مدیریت WordPress شوید

3. به **ظاهر > قالب‌ها** بروید

4. قالب "Art of Iran" را پیدا کنید و **فعال** کنید

### ۴. تنظیمات اولیه

پس از فعال‌سازی:

1. به **ظاهر > سفارشی‌ساز** بروید
2. در بخش **تنظیمات قالب** پیام خوش‌آمدگویی را تنظیم کنید
3. در صورت نیاز، لوگو را آپلود کنید

### ۵. نصب افزونه‌های مورد نیاز (اختیاری)

برای کارکرد کامل:
- **WooCommerce** (برای فروشگاه)
- **Dokan** (برای چند فروشنده)

## عیب‌یابی خطاهای رایج

### خطای Critical Error

اگر خطای critical دریافت کردید:

1. **استفاده از functions-minimal.php:**
   ```bash
   cd /wp-content/themes/artofiran/
   cp functions-minimal.php functions.php
   ```

2. **بررسی مجوزات فایل:**
   ```bash
   chmod 644 *.php *.css
   chmod 755 woocommerce/
   ```

3. **فعال‌سازی حالت دیباگ:**
   در فایل `wp-config.php` اضافه کنید:
   ```php
   define('WP_DEBUG', true);
   define('WP_DEBUG_LOG', true);
   ```

### مشکل نمایش فونت فارسی

اگر فونت فارسی نمایش داده نمی‌شود:

1. اتصال اینترنت را بررسی کنید
2. CDN فونت Vazir در دسترس باشد
3. فایل `rtl.css` بارگذاری شده باشد

### مشکل قالب‌بندی RTL

1. اطمینان حاصل کنید زبان سایت روی فارسی تنظیم شده
2. فایل `rtl.css` موجود باشد
3. در تنظیمات WordPress، **جهت متن** روی راست به چپ باشد

## فایل‌های کمینه شده

در صورت بروز مشکل، از این فایل‌های ساده‌شده استفاده کنید:

### functions.php (کمینه)
```php
<?php
if (!defined('ABSPATH')) exit;

function artofiran_setup() {
    add_theme_support('post-thumbnails');
    add_theme_support('title-tag');
    if (class_exists('WooCommerce')) {
        add_theme_support('woocommerce');
    }
}
add_action('after_setup_theme', 'artofiran_setup');

function artofiran_styles() {
    wp_enqueue_style('theme-style', get_stylesheet_uri());
    if (is_rtl()) {
        wp_enqueue_style('theme-rtl', get_template_directory_uri() . '/rtl.css');
    }
}
add_action('wp_enqueue_scripts', 'artofiran_styles');
?>
```

### index.php (ساده)
```php
<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo('charset'); ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v30.1.0/dist/font-face.css" rel="stylesheet">
    <?php wp_head(); ?>
</head>
<body <?php body_class(); ?>>

<header>
    <div class="site-branding">
        <h1><a href="<?php echo home_url(); ?>"><?php bloginfo('name'); ?></a></h1>
    </div>
</header>

<main>
    <?php if (have_posts()): while (have_posts()): the_post(); ?>
        <article>
            <h2><a href="<?php the_permalink(); ?>"><?php the_title(); ?></a></h2>
            <div><?php the_content(); ?></div>
        </article>
    <?php endwhile; endif; ?>
</main>

<footer>
    <p>&copy; <?php echo date('Y'); ?> <?php bloginfo('name'); ?></p>
</footer>

<?php wp_footer(); ?>
</body>
</html>
```

## تست نهایی

پس از نصب:

1. صفحه اصلی سایت را باز کنید
2. بررسی کنید قالب بدون خطا بارگذاری می‌شود
3. فونت فارسی درست نمایش داده می‌شود
4. منوها و ناوبری کار می‌کند

## پشتیبانی

در صورت مشکل:

1. لاگ خطاها را بررسی کنید: `/wp-content/debug.log`
2. از حالت دیباگ WordPress استفاده کنید
3. افزونه‌های غیرضروری را غیرفعال کنید
4. به قالب پیش‌فرض برگردید و مجدداً تست کنید

---

**نکته مهم:** همیشه قبل از تغییر قالب، پشتیبان از سایت تهیه کنید!